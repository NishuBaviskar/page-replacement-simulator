# Dependencies
# This folder contains all the downloaded packages and can be very large.
# It should always be regenerated by running `npm install`.
/node_modules

# Vite build output
# This folder is created when you run `npm run build` and contains the final, optimized code for deployment.
/dist

# Logs
# These files are generated by package managers during errors.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
# These files often contain sensitive information like API keys and should never be committed.
.env
.env.local
.env.*.local

# IDE config
# These folders are specific to your code editor (like VS Code or WebStorm) and aren't needed by other developers.
.vscode/
.idea/